# JDBCmini_project
This is a basic Java JDBC project that connects a Swing-based GUI with a database. It includes modules for login, insert, delete, search, and display operations. The project demonstrates JDBC connectivity, SQL execution, event handling, and modular design using multiple classes.
